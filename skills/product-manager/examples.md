# 产品经理（product-manager）示例

四个模式的完整示例：M1 一次成型需求定义 / M2 产品体验走查 / M3 缺失功能推演 / M4 界面视觉走查（L3 手动上传降级），外加示例 5：代码直读核验（不跑服务直接判定符合性）。

---

## 示例 1：M1 一次成型 —— 用户提一个功能想法

**用户输入**：「我想加个导出功能，把报表导出成 Excel。」

**档位判定**：单点功能、1 个角色、无外部依赖 → 一次成型档。

**AI 行为**：自主推理 → 内联负向场景 → 出报告 → **只问一次**（关键假设确认）。

```markdown
# PM 报告：报表导出

> 证据声明：🟡 静态可证 · 未实测（产品未运行）· 现状读代码确认

## 1. 执行摘要
- 真实需求是「把报表数据带走做二次加工」，导出 Excel 只是方案之一（🟡）
- 核心风险不在"能不能导出"，而在**大数据量导出的等待与失败恢复**（🟡）
- 建议优先做异步导出 + 进度可查，而非同步下载（🟡）

## 2. 现状与范围
- 边界：报表列表页与详情页的导出入口
- 现状来源：读代码确认（无导出相关代码，`report/` 下仅有查询接口）
- 未覆盖：导出文件的权限控制（待确认）

## 3.0 影响面（M1 必填）
| 受影响功能/方 | 影响类型 | 影响程度 | 证据 | 建议处理 | 需用户拍板 |
|--------------|---------|---------|------|---------|-----------|
| 报表页面的列配置 | 数据/展示 | 降级 | 🟡 列配置与导出字段各有一份映射 | 本次一并改（REQ-04） | 否 |
| 导出权限 | 权限 | 破坏 | 🟢 当前无任何导出鉴权，任何人可导出全量 | 本次一并改（REQ-05） | 否 |
| 定时报表邮件任务 | 流程 | 无感 | ⚪ 未发现（需确认） | 需确认是否存在 | 是 |
| 对外 API /v1/reports | 外部契约 | 无感 | 🟢 纯新增能力，不改现有契约 | 无需处理 | 否 |

> ⚪ = 未发现；不得默认"无影响"，已列入待确认。

## 3. 需求清单
| ID | 需求 | 预期效果 | 优先级 | 验收标准 |
|----|------|---------|--------|---------|
| REQ-01 | 报表导出 Excel | 点击后拿到与页面一致的 Excel | P0 | 导出数据与页面筛选条件一致 |
| REQ-02 | 异步导出 + 进度 | 大数据量时不卡死页面，可查进度 | P0 | 10 万行导出页面不阻塞，进度可见 |
| REQ-03 | 导出失败可重试 | 失败后能看到原因并重试 | P1 | 失败有原因说明，一键重试 |
| REQ-04 | 导出字段与页面列配置统一 | 页面加列后导出自动跟随，不再两处维护 | P1 | 加一列后导出含该列，无需改代码 |
| REQ-05 | 导出权限管控 | 无权限者看不到导出入口、接口亦拒绝 | P0 | 越权调用导出接口返回 403 |

> REQ-04 / REQ-05 **来自影响面**——影响项必须变成需求条目，只写在说明里必然漏做。

## 3.2 负向需求
| ID | 场景 | 类型 | 预期程序行为 |
|----|------|------|-------------|
| NEG-01 | 点了导出没反应以为没生效 | 状态异常 | 立即置为"准备中"，禁用按钮防重复点 |
| NEG-02 | 导出中关闭页面 | 中途放弃 | 后台继续，回来能查到历史导出 |
| NEG-03 | 无数据 / 筛选结果为空 | 数据异常 | 提示"当前条件下无数据可导出"，不生成空文件 |
| NEG-04 | 导出超时（大数据量） | 状态异常 | 转异步并通知，不让用户干等 |

## 4. 优先级排序
| 优先级 | 条目 | Kano | 价值 | 成本 | 证据 | 排序理由 |
|--------|------|------|------|------|------|---------|
| P0 | REQ-01 导出 | 必备 | 高 | 中 | 🟡 | 无此能力用户只能手工抄 |
| P0 | REQ-02 异步+进度 | 必备 | 高 | 中 | 🟡 | 同步导出大数据量必然超时，属必备 |
| P0 | REQ-05 导出权限管控 | 必备 | 高 | 中 | 🟢 | 无鉴权即越权导出全量数据，属必备 |
| P1 | REQ-03 失败重试 | 期望 | 中 | 低 | 🟡 | 提升容错，成本低 |
| P1 | REQ-04 字段配置统一 | 期望 | 中 | 中 | 🟡 | 防两处维护漂移，非阻塞 |

## 5. 待确认清单
（本次无待确认项——所有结论均有代码或静态证据支撑；H-01～H-03 见下节假设表）

## 6. 关键假设（唯一确认点）
| ID | 假设 | 错了会怎样 | 验证方法 |
|----|------|-----------|---------|
| H-01 | 导出量级在万行以上 | 若仅百行，同步导出即可，REQ-02 降级 | 询问用户典型数据量 |
| H-02 | 导出内容与页面筛选一致 | 若需独立配置导出字段，需求翻倍 | 确认导出范围 |
| H-03 | 无需按角色限制导出权限 | 若需限制，要加权限设计 | 确认是否存在敏感数据 |

> 请重点确认 H-01～H-03，其余我已按最可能情况填好。
```

---

## 示例 2：M2 产品体验走查（含优先级表）

**用户输入**：「体验一下这个后台管理系统，看看还能优化什么。」

```markdown
# PM 报告：后台管理系统体验走查

> 证据声明：🟢 实测 · 已实际走查 6 个页面

## 1. 执行摘要
- 核心路径「新建 → 提交 → 查结果」能走通，但**提交后无任何反馈**，用户会重复提交（🟢）
- 列表页无保存的筛选条件，运营每天重复配置 5 次筛选（🟢）
- 空态只有"暂无数据"，新用户不知道下一步该干什么（🟢）

## 3.0 影响面（M2 必填）
| 受影响功能/方 | 影响类型 | 影响程度 | 证据 | 建议处理 | 需用户拍板 |
|--------------|---------|---------|------|---------|-----------|
| 提交后的下游状态流转 | 流程 | 无感 | 🟢 仅加前端反馈，不动状态机 | 无需处理 | 否 |
| 运营的每日筛选 SOP | 流程 | 降级 | 🟡 筛选记忆会改变默认视图 | 需确认：默认是否保留全量 | 是 |
| 移动端（若有） | 展示 | 未知 | ⚪ 未发现移动端入口（需确认） | 需确认 | 是 |

## 3. 发现清单
| ID | 现象 | 影响谁 | 证据 | 代码位置 |
|----|------|--------|------|---------|
| F-01 | 提交后按钮无 loading、无成功提示 | 所有用户 | 🟢 screenshot-2 | `src/pages/form.tsx:88` |
| F-02 | 筛选条件刷新后丢失 | 运营 | 🟢 实测复现 | `src/pages/list.tsx:41` |
| F-03 | 空态无引导 | 新用户 | 🟢 screenshot-5 | `src/components/Empty.tsx` |
| F-04 | 删除与停用两种操作交互不一致 | 所有用户 | 🟡 代码比对 | - |

经走查排除：权限控制维度——越权访问均已正确拦截（🟢）。

## 4. 优先级排序
| 优先级 | 条目 | 来源 | Kano | 价值 | 成本 | 证据 | 排序理由 |
|--------|------|------|------|------|------|------|---------|
| P0 | F-01 提交反馈 | M2 | 必备 | 高 | 低 | 🟢 | 无反馈会导致重复提交，核心路径必备 |
| P1 | F-02 筛选记忆 | M2 | 期望 | 高 | 中 | 🟢 | 高频重复操作，需存偏好故成本中 |
| P1 | F-03 空态引导 | M2 | 期望 | 中 | 低 | 🟢 | 新用户转化，改动小 |
| P2 | F-04 交互一致性 | M2 | 期望 | 低 | 中 | 🟡 | 影响有限，可随其他改动一并处理 |

## 7. 建议下一步
1. `request-guard` 确认 F-01/F-02 改造范围 → `code-review` 实施后复查
2. 如需覆盖更多角色路径，可发起 M3 缺失功能推演
```

---

## 示例 3：M3 缺失功能推演（含 🔴 待确认）

**用户输入**：「从代码看看这个产品还缺什么功能。」

```markdown
# PM 报告：缺失功能推演

> 证据声明：混合（🟢 代码实证 / 🟡 结构可证 / 🔴 推断）· 产品未运行

## 3.0 影响面（M3 必填）
| 受影响功能/方 | 影响类型 | 影响程度 | 证据 | 建议处理 | 需用户拍板 |
|--------------|---------|---------|------|---------|-----------|
| 报表页已有筛选 | 数据 | 降级 | 🟢 导出须与筛选一致 | 本次一并改（G-01 内） | 否 |
| 导出权限点 | 权限 | 破坏 | ⚪ 未发现权限配置（需确认） | 需确认：谁能导出 | 是 |
| 审计日志表 | 数据 | 无感 | 🟡 `audit_log` 已建但未写入 | 单独立项（G-02） | 否 |

> M3 是"缺什么功能"，**补功能同样是改动**，影响面照样必填——不能因为是"新增"就默认不影响存量。

## 3. 发现清单
| ID | 缺失功能 | 证据信号 | 证据 |
|----|---------|---------|------|
| G-01 | 导出 | `ExportButton` 组件 onClick 为空实现 | 🟢 |
| G-02 | 操作审计 | `audit_log` 表已建，无任何写入调用 | 🟡 |
| G-03 | 批量操作 | 仅有单条接口，无批量路由 | 🟡 |
| G-04 | 数据回收站 | 删除为物理删除，无软删字段 | 🟡 |

## 4. 优先级排序
| 优先级 | 条目 | 来源 | Kano | 价值 | 成本 | 证据 | 排序理由 |
|--------|------|------|------|------|------|------|---------|
| P0 | G-01 导出 | M3 | 必备 | 高 | 中 | 🟢 | 入口已建却无实现，用户点了没反应 |
| P1 | G-02 审计 | M3 | 必备 | 中 | 中 | 🟡 | 表已建说明原计划有，合规场景必备 |
| P2 | G-03 批量操作 | M3 | 期望 | 中 | 中 | 🟡 | 提效项，非阻塞 |
| P2 | G-04 回收站 | M3 | 期望 | 中 | 高 | 🟡 | 需改删除语义与数据迁移，成本高 |

## 5. 待确认清单
| # | 待确认项 | 为什么存疑 | 验证方法 |
|---|---------|-----------|---------|
| 1 | 国际化是否是**有意不做** | 仅中文文案，可能是 MVP 取舍而非遗漏 | 查 backlog / 问产品负责人 |
| 2 | 是否计划支持批量导入 | 同类 SaaS 普遍提供（参照：{具体产品}），代码无痕迹 | 问产品负责人 |
| 3 | 消息通知中心 | 推断项，无代码信号支撑 | 查需求文档 |
| 4 | 导出权限配置（影响面 ⚪） | 未发现权限点，影响"谁能导出"的判定 | 查权限配置 / 问产品负责人 |
```

---

## 示例 4：代码直读核验（不看画面，直接判定符合性）

**用户输入**：「检查一下这个系统的删除操作是不是都有二次确认。」

**判定类型**：符合性 → **首选代码直读**，不起服务、不要截图。

```markdown
# PM 报告：删除操作二次确认核验

> 证据声明：🟢 代码直读实证 · 取证方式：代码直读（本判定无需运行）· 已穷举 6 处删除入口

## 1. 执行摘要
- 6 处删除入口中 **2 处无任何二次确认**，其中 1 处是不可逆的物理删除（🟢）
- 3 处有确认弹窗，但文案均未说明"删除后不可恢复"（🟢）
- 1 处仅有前端确认、接口层无校验，存在绕过风险（🟢）

## 2. 检查项与判定
| 检查项 | 判定 | 证据（file:line） | 备注 |
|--------|------|------------------|------|
| 用户列表删除 | ✅ 符合 | `src/pages/user.tsx:112` Popconfirm | 文案未提不可恢复 |
| 角色删除 | ❌ 不符合 | `src/pages/role.tsx:67` onClick 直接调接口 | 不可逆，风险最高 |
| 订单作废 | ❌ 不符合 | `src/pages/order.tsx:203` | 直接调用，无确认 |
| 批量删除 | ⚠️ 部分符合 | `src/pages/user.tsx:145` | 有确认但未列数量，用户不知删了几个 |
| 接口层校验 | ⚠️ 部分符合 | `src/api/role.ts:31` 无二次校验 | 前端确认可被绕过 |

## 3.0 影响面
`-`（本次为纯符合性核验、不提出改动，影响面不适用；若据此提出整改需求，则须补影响评估）

## 4. 优先级排序
| 优先级 | 条目 | Kano | 价值 | 成本 | 证据 | 排序理由 |
|--------|------|------|------|------|------|---------|
| P0 | 角色删除加确认 | 必备 | 高 | 低 | 🟢 | 不可逆操作无确认，误点即事故 |
| P0 | 订单作废加确认 | 必备 | 高 | 低 | 🟢 | 同上，业务影响大 |
| P1 | 接口层补校验 | 必备 | 中 | 中 | 🟢 | 前端确认可绕过，需服务端兜底 |
| P2 | 确认文案补"不可恢复" | 期望 | 中 | 低 | 🟢 | 降低误删，改动小 |

## 5. 待确认清单
| # | 待确认项 | 验证方法 |
|---|---------|---------|
| 1 | 订单作废是否业务上允许无确认（高频操作，可能有意为之） | 问产品/业务负责人 |
| 2 | 是否有软删除机制（若有则"不可逆"结论需修正） | 查删除接口实现 |

## 7. 建议下一步
1. `request-guard` 确认改动范围 → `code-review` 实施
2. 若要看确认弹窗的实际观感与文案，可发起 M4 界面走查（本例无需）
```

> 全程未起服务、未要截图——"有没有二次确认"是代码事实，读代码 30 秒即可判定。

---

## 示例 5：M4 界面视觉走查（L3 手动上传降级）

**用户输入**：「看看这个页面怎么样。」（环境无自动截图能力）

**AI 行为**：先能力探测 → 判定 L3 → 给拍摄清单 → 用户上传后走查。

```markdown
环境未检出自动截图能力，无法直接读取页面渲染结果。为给出准确结论，请提供截图：

1. 首页/主界面（有数据状态）
2. 核心操作流程的中间步骤
3. 空状态（无数据）
4. 错误态（随便填错触发）
5. 窄屏视图（375px 宽）
要求：桌面 ≥1440px 宽，整屏截图。缺哪张告诉我就行，我会按可得画面降级。
```

**用户上传 3 张后**：

```markdown
# PM 报告：{页面} 界面走查

> 证据声明：🟢 截图实测（用户上传 3 张）· 未获得：错误态、窄屏视图（相关结论标 🔴/不输出）

## 1. 执行摘要
- 主操作按钮与次要按钮视觉权重接近，一屏内看不出首选操作（🟢）
- 空态只有"暂无数据"，无引导、无操作入口（🟢）
- 卡片间距存在 12px / 16px 混用，视觉不够整齐（🟢）
- ⚠️ 未获得错误态与窄屏截图，以下不包含这两个维度的结论

## 3. 发现清单
| ID | 现象 | 证据 | 建议方向 |
|----|------|------|---------|
| U-01 | 主次按钮权重接近 | 🟢 screenshot-1 | 主操作用实心主色，次要用描边 |
| U-02 | 空态无引导 | 🟢 screenshot-3 | 空态加入口按钮与一句说明 |
| U-03 | 间距 12/16px 混用 | 🟢 screenshot-1 | 统一到设计 token |

经走查排除：文案术语维度——全页术语一致，无"删除/移除"混用（🟢）。

## 4. 优先级排序
| 优先级 | 条目 | Kano | 价值 | 成本 | 证据 | 排序理由 |
|--------|------|------|------|------|------|---------|
| P1 | U-01 按钮层级 | 期望 | 中 | 低 | 🟢 | 影响操作效率，改样式即可 |
| P1 | U-02 空态引导 | 期望 | 中 | 低 | 🟢 | 新用户留存，成本低 |
| P2 | U-03 间距统一 | 期望 | 低 | 低 | 🟢 | 观感问题，可随其他改动处理 |

## 5. 待确认清单
| # | 待确认项 | 验证方法 |
|---|---------|---------|
| 1 | 错误态是否已有反馈文案（未获得截图） | 补一张错误态截图 |
| 2 | 窄屏是否折行/溢出 | 补一张 375px 截图 |
| 3 | U-01 改按钮样式是否为**共用组件**（若是则影响面须重评） | 查该按钮组件被引用的页面 |
```
