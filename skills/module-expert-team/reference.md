# 模块专家团 · 切面调研清单与模板

> 子 agent 加载本文件，按所属切面执行调研并按模板产出 `.module-experts/{中文专家名}/{切面}.md`。`agent.md` 由主 agent 在 Step 4 合成，子 agent 不产出。

## 通用要求

- 每篇文档顶部写：专家名 / 模块名 / 模块根路径 / 信息来源（ki / 代码搜索 / 语义检索）
- 关键结论后附**文件路径**（相对仓库根），便于回溯
- 无内容的章节标注「该模块无此项」，不留空、不编造
- 不逐行转储代码，提炼模式与要点
- `agent.md` 由主 agent 在 Step 4 合成（专家名片），子 agent 不产出此文件

---

## agent.md（专家名片）— 必出

> 总结"这个专家是干嘛的"，作为专家入口名片，供后续查找与加载时快速判断是否匹配需求。比 architecture 更短、更偏"职责定位"而非"技术细节"。由主 agent 在 Step 4 基于全部切面产出合成。

### 产出模板
```markdown
# 专家：{中文专家名}

## 一句话职责
{该专家负责的**业务领域**}

## 负责的模块
- {模块根路径}：{一句话职责}

## 何时找这个专家
- {场景 1}
- {场景 2}

## 包含的资产
- 切面文档：architecture, implementation, data-flow（如启用）, ...
- 专用技能：skills/{skill1}（或「暂无专用技能」）

## 出处
- 生成日期：{date}  git commit：{hash}
```

---

## architecture（架构）— 必出

### 调研清单
- 模块对外的职责边界：做什么、不做什么
- 子模块 / 包划分：每个子模块的路径 + 一句话职责
- 分层结构（如 controller / service / repo / model）
- 依赖关系：依赖哪些内部模块 / 外部库；被谁依赖
- 关键设计约束 / 不变量（如"所有写操作必经 X"）

### 产出模板
```markdown
# 架构：{专家名} / {模块名}

> 来源：{ki / 代码搜索}  根路径：{root}

## 一句话职责
{...}

## 子模块地图
| 子模块 | 路径 | 职责 |
|---|---|---|

## 分层与依赖
{分层图 / 依赖说明，标注关键文件}

## 设计约束与不变量
- {...}

## 切面索引（architecture 预置，主 agent 在 Step 4 补全）
- 实现：./implementation.md
- 数据流转：./data-flow.md（如启用）
- 模型：./models.md（如启用）
- 接口：./api.md（如启用）
- 测试：./tests.md（如启用）
- 运维：./ops.md（如启用）

## 关键发现 / 风险 / 常见坑（主 agent 在 Step 4 补全）
- {...}

## 术语表
- {术语}：{释义}

## 专用技能（主 agent 在 Step 5 补全）
- skills/{skill1}：{一句话用途}（或「暂无专用技能」）

## 出处
- 生成日期：{date}  git commit：{hash}
```

---

## implementation（实现）— 必出

### 调研清单
- 核心算法 / 关键流程的实现位置
- 所用设计模式（工厂 / 策略 / 观察者 等）及位置
- 热点路径：高频调用 / 性能敏感代码
- 关键类 / 函数清单（名称 + 路径 + 职责）
- 已知技术债 / hack 注释

### 产出模板
```markdown
# 实现：{专家名} / {模块名}

> 来源：{...}

## 核心流程
{流程 + 关键文件路径}

## 设计模式
| 模式 | 位置 | 用途 |

## 关键类 / 函数
| 名称 | 路径 | 职责 |

## 热点路径
- {...}

## 技术债
- {...}
```

---

## data-flow（数据流转）— 有 api 或 db 时启用

### 调研清单
- 主入口 → 出口的完整数据生命周期
- 请求 / 事件 / 消息的流转路径
- 状态机 / 状态流转
- 异步流（队列 / 定时任务 / 回调）
- 数据变换关键节点

### 产出模板
```markdown
# 数据流转：{专家名} / {模块名}

> 来源：{...}

## 主流程数据生命周期
{入口 → 变换 → 出口，标注文件}

## 状态流转
{状态机或状态列表}

## 异步流
- {...}
```

---

## models（模型）— 有 db 时启用

### 调研清单
- 数据模型 / 实体定义位置
- DB schema / 表结构
- ORM 映射方式
- 迁移文件位置与惯例
- 校验规则（必填 / 格式 / 业务约束）

### 产出模板
```markdown
# 模型：{专家名} / {模块名}

> 来源：{...}

## 实体清单
| 实体 | 路径 | 关键字段 |

## DB schema
{表 / 集合概述}

## 迁移
- 位置：{...}  惯例：{...}

## 校验规则
- {...}
```

---

## api（接口）— 有 api 时启用

### 调研清单
- 对外公开入口（HTTP / RPC / CLI / 事件）
- 请求 / 响应契约
- 错误码与错误响应格式
- 版本策略
- 鉴权 / 限流约定

### 产出模板
```markdown
# 接口：{专家名} / {模块名}

> 来源：{...}

## 入口清单
| 入口 | 路径 | 方法 | 说明 |

## 契约示例
{请求 / 响应}

## 错误码
| 码 | 含义 |

## 版本与鉴权
- {...}
```

---

## tests（测试）— 有测试时启用

### 调研清单
- 单元测试位置（目录 / 文件命名惯例）
- 测试框架与组织方式
- 断言风格 / mock 方式
- 如何运行（命令）
- 覆盖情况与缺口

### 产出模板
```markdown
# 测试：{专家名} / {模块名}

> 来源：{...}

## 测试位置
- 目录：{...}  命名惯例：{...}

## 框架与组织
- {...}

## 运行命令
\`\`\`bash
{命令}
\`\`\`

## 覆盖与缺口
- {...}
```

---

## ops（运维）— 有配置时启用

### 调研清单
- 配置文件组织 / 环境变量
- 部署相关（Dockerfile / k8s / CI）
- feature flag
- 日志框架 / 级别 / 关键日志点
- 监控 / 指标接入

### 产出模板
```markdown
# 运维：{专家名} / {模块名}

> 来源：{...}

## 配置
- 文件：{...}  环境变量：{...}

## 部署
- {...}

## 日志与监控
- {...}
```

---

## Step 5：识别专用技能的指引

主 agent 在 Step 4 完成后，基于全部切面文档识别**该业务领域具体、可复用的操作型任务**：

- 判定标准：是一个会被反复执行、有明确步骤、值得固化成技能的操作（如"新增告警规则""排查告警不触发""查询历史日志"）
- 每识别出一个 → 调用 `create-skill` 技能生成标准 `SKILL.md`，落盘到 `.module-experts/{中文专家名}/skills/{kebab-skill-name}/`
- 技能内容须含真实触发短语与可执行步骤；无价值操作任务时留空 `skills/` 并在 architecture.md 注明
- 技能格式一律以 create-skill 规范为准

---

## 子 agent prompt 模板

```
你是子 agent，负责切面 {NN}-{切面}：{切面名}

## 模块
- 专家名（中文）：{中文专家名}
- 模块根路径：{module_root}
- 模块名：{module-name}

## 调研清单与模板
见 reference.md 中「{切面}」章节

## 同批产出的其他切面（供 architecture 预置切面索引）
{列出同批切面清单，如 implementation, api, tests}

## 方法
1. search_content / search_file 精确定位（优先）
2. read_file 读关键文件
3. 信息来源标注：ki / 代码搜索 / 语义检索

## 产出
先写到 .codebuddy/task-dispatch/module-expert-{专家简称}/subtasks/{NN}-{切面}/{切面}.md
完成后落盘到 .module-experts/{中文专家名}/{切面}.md
并写 report.md 说明搜索范围与关键发现
```
