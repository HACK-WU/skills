---
name: PROJECT
type: project-overview
updatedAt: {date}
维护者: expert-team / expert-lookup
---

# {项目名} 项目全局

## 1. 项目基本信息
- **业务领域**：{一句话领域描述}
- **项目用途**：{项目解决什么问题}

## 2. 技术栈
- 后端：{语言/框架/版本}
- 前端（如有）：{框架}（目录位置：{path}，无前端则省略）
- 中间件/存储：{DB / MQ / Redis 等}

## 3. 架构形态
- **前后端**：{前后端分离 / 单体 / 混合}
- **部署形态**：{多服务独立部署 / 单体部署 / 混合}

## 4. 核心功能
- {功能1：一句话}
- {功能2：一句话}

## 5. 核心服务清单（本仓库内）
| 服务/模块名 | 一句话职责 | 代码位置 | 测试可执行性 |
|---|---|---|---|
| {服务1} | {职责} | {目录/包路径} | ✅ 可跑 / ⚠️ 依赖外部环境 / ❌ 无法运行 / 无测试 |

> 测试可执行性标注「❌ 无法运行 / 无测试」的服务，创建/使用其专家时**跳过测试内容**（不产出/不加载 `06-测试.md` 与 `test/known-failures.md`）。

## 6. 配套服务关系（独立部署的配套服务）
| 服务A | 关系 | 服务B | 说明 |
|---|---|---|---|
| {服务A} | 依赖/被依赖/协作 | {服务B} | {关系描述} |

## 7. 运行环境（解释器/执行方式）
| 场景 | 推荐方式 | 说明 |
|---|---|---|
| Python 执行 | `uv run python xxx.py` 或 `{虚拟环境路径}/bin/python` | 直接 `python` 常因未激活虚拟环境而失败 |
| {其他运行时} | {推荐执行命令} | {说明} |

## 8. 架构图（服务级）
```mermaid
graph LR
    A[{服务A}] --> B[{服务B}]
```

## 9. 数据流向图（服务级）
```mermaid
flowchart LR
    A[{入口}] --> B[{服务A}] --> C[(存储)]
```
