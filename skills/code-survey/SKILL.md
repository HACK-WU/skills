---
name: code-survey
description: 在设计前对代码库进行按需调研，只调研与当前需求相关的维度（代码风格、路径、架构、类似功能、技术栈、数据存储、错误处理、安全、日志、API、测试、配置、部署）。优先从记忆系统/知识库获取信息，不足时再搜索代码库。适用于"代码调研"、"了解现有代码"、"项目代码风格"、"代码路径"、"架构调研"等场景，或在 design-craft 等技能的设计前置阶段被调用。
---

# 代码现状调研

## 概述

**目的**：在设计前充分了解当前代码库中与需求相关的信息，确保设计与现有代码风格、架构保持一致，避免设计出与项目现状脱节的方案

**功能**：按需调研代码库的 13 个维度（代码风格、代码路径、架构模式、类似功能、技术栈、数据存储、错误处理、安全惯例、日志与监控、API 约定、测试模式、配置管理、部署与 CI/CD），根据需求特征自动筛选需要调研的维度，优先从知识库/记忆系统获取信息

**使用场景**：
- design-craft 技能执行前置信息收集阶段时
- 任何需要在设计前了解现有代码的场景
- 用户说"了解一下现有代码"、"代码风格是什么"、"这个功能的代码在哪里"时

## 核心原则

1. **按需调研**：只调研与当前需求相关的维度，不相关的跳过
2. **信息复用优先**：记忆系统 → 知识库 → 已有文档 → 代码搜索，禁止跳过前置步骤直接搜代码
3. **必须执行**：记忆系统和知识库为必查项，即使觉得“不太可能有”，也要实际调用确认

## 快速跳过判定

满足以下任一条件时，可极简化或跳过本步骤：

- 上下文成熟度为「高」（用户已充分讨论过技术方案）
- 用户明确表示已熟悉项目代码
- 知识库/记忆系统中已有充分的项目架构知识

## 调研维度

**关键：根据需求特征选择维度，只调研需求涉及的部分。** 表中"需求特征"列是触发条件——需求命中该特征时才调研对应维度。

| 维度 | 调研内容 | 需求特征（命中才调研） |
|------|----------|----------------------|
| **代码风格** | 命名规范、代码组织方式、注释风格 | 首次接触该项目 |
| **代码路径** | 与需求相关的现有代码文件、模块位置 | 不清楚需求涉及的模块 |
| **架构模式** | 项目整体架构、模块划分、依赖关系 | 对项目架构不熟悉 |
| **类似功能** | 已有的类似功能实现，可参考的模式 | 不确定项目是否已有参考 |
| **技术栈** | 使用的框架、库、版本约束（可引用 dependencies/） | 首次接触该项目 |
| **数据存储** | 数据库模式、ORM 用法、迁移惯例 | 需求涉及数据模型变更 |
| **错误处理** | 异常处理惯例、错误码规范、重试策略 | 需求涉及异常/边界处理 |
| **安全惯例** | 认证方式、权限模型、输入校验模式 | 需求涉及权限或数据安全 |
| **日志与监控** | 日志框架、日志级别、监控接入方式 | 需求涉及可观测性内容 |
| **API 约定** | REST/GraphQL 风格、版本策略、分页与错误响应格式 | 需求涉及新增或修改 API |
| **测试模式** | 测试框架、测试组织方式、断言风格 | 需求包含测试相关内容 |
| **配置管理** | 配置文件组织、环境变量管理、feature flag | 需求涉及配置或环境差异 |
| **部署与 CI/CD** | 部署流程、CI 流水线、发布策略 | 需求涉及部署变更 |

### 与依赖文档的关系

技术栈维度应优先引用已整理的依赖文档（`dependencies/`），避免重复调研。仅当依赖文档未覆盖时才补充搜索。

### 调研示例

**需求："给用户列表接口增加分页功能"**
- 命中维度：代码路径（找用户列表接口）、API 约定（分页格式）、类似功能（是否已有分页实现）
- 跳过维度：安全惯例、部署与 CI/CD、测试模式（如需求未提及测试）

**需求："重构认证模块，从 session 迁移到 JWT"**
- 命中维度：代码路径、架构模式、安全惯例、数据存储（token 存储）、错误处理
- 跳过维度：API 约定（如不涉及 API 变更）、部署与 CI/CD

## 调研执行顺序（强制）

**禁止跳过步骤直接搜代码。** 必须按顺序逐级执行，每一步都实际调用对应工具并记录结果，再决定是否需要进入下一步。

### 第 1 步：查询记忆系统

**必须执行。** 使用 `memory_recall` 查询与当前需求/项目相关的历史记忆。

- 目的：获取之前调研过的结论，避免重复劳动
- 若命中：直接复用记忆内容，跳过对应维度的代码搜索
- 若未命中：记录「记忆系统无相关结果」，进入第 2 步

### 第 2 步：查询知识库

**必须执行。** 使用 `ki_search` 语义搜索代码知识库，查询与需求相关的模块信息、架构知识、API 接口等。

- 目的：获取已结构化的代码知识，比直接搜代码更精准
- 若命中：提取相关模块信息，跳过对应维度的代码搜索
- 若未命中：记录「知识库无相关结果」，进入第 3 步

### 第 3 步：检查已有文档

检查需求目录下是否已有相关文档（`dependencies/`、`reference/` 等）。

- 目的：复用已有产出，避免重复生成
- 若命中：引用已有文档内容
- 若未命中：进入第 4 步

### 第 4 步：代码库搜索（兜底）

**仅当以上 3 步均无法覆盖某个维度时，才执行代码搜索。**

### 产出物中的溯源要求

产出文档必须标注每个维度的信息来源：

```
> 调研来源：记忆系统 ✓ | 知识库 ✓ | 已有文档 ✗ | 代码搜索 ✓
```

这确保调研过程可追溯，也方便后续复用。

## 产出物

生成简略的 **代码调研文档**，**必须落盘到文件系统**，不要只在对话中输出。

### 落盘规范

- **存储路径**：需求目录的 `reference/` 子目录下，文件名 `code-survey.md`
- **落盘方式**：使用 `write_to_file` 写入文件，确保文档持久化
- **文件命名**：`reference/code-survey.md`（如已有同名文件则覆盖更新）

### 文档要求

- 仅记录实际调研过的维度，未调研的维度不写入文档
- 每个维度标注信息来源（记忆系统 / 知识库 / 已有文档 / 代码搜索）
- 内容简略，重点是帮助设计决策，不需要非常详细

### 文档模板

```markdown
# 代码调研：{功能名称}

> 调研来源：知识库 / 记忆系统 / 代码库搜索（标注实际使用的来源）
> 调研范围：仅列出与需求相关的维度

## 1. 代码风格（如已调研）
- 命名规范：[简述]
- 代码组织：[简述]

## 2. 相关代码路径（如已调研）
- [文件路径1]：[简要说明]

## 3. 架构概览（如已调研）
- 整体架构：[简述]

## 4. 可参考的类似功能（如已调研）
- [功能/文件]：[参考价值]

## 5. 技术栈约束（如已调研，可引用 dependencies/）
- 框架：[版本]

## 6. 数据存储（如已调研）
- 数据库/ORM：[简述]
- 迁移惯例：[简述]

## 7. 错误处理（如已调研）
- 异常模式：[简述]
- 错误码规范：[简述]

## 8. 安全惯例（如已调研）
- 认证方式：[简述]
- 权限模型：[简述]

## 9. 日志与监控（如已调研）
- 日志框架：[简述]

## 10. API 约定（如已调研）
- 风格：[REST/GraphQL]
- 错误响应格式：[简述]

## 11. 测试模式（如已调研）
- 框架与组织：[简述]

## 12. 配置管理（如已调研）
- 配置方式：[简述]

## 13. 部署与 CI/CD（如已调研）
- 流程：[简述]

## 14. 其他调研发现（按需）
- [维度]：[发现]
```

> **重要**：代码调研文档作为设计参考，不需要非常详细，但应包含足够的信息帮助设计决策。已知信息不重复调研，仅补充未知部分。**只写与需求相关的维度，无关维度不要出现在文档中。**
